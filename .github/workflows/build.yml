name: Build APK (Gradle)

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type: debug or release'
        required: false
        default: 'release'

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install JS dependencies
        run: npm ci
        working-directory: supermobile-app

      - name: Set Android SDK local.properties
        run: echo "sdk.dir=$ANDROID_HOME" > supermobile-app/android/local.properties

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('supermobile-app/android/**/*.gradle*', 'supermobile-app/android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Make gradlew executable
        run: chmod +x supermobile-app/android/gradlew

      - name: Build APK
        run: |
          BUILD_TYPE="${{ github.event.inputs.build_type || 'release' }}"
          TASK="assemble$(echo ${BUILD_TYPE^})"
          cd supermobile-app/android && ./gradlew $TASK --no-daemon --stacktrace

      - name: Find and upload APK
        run: |
          BUILD_TYPE="${{ github.event.inputs.build_type || 'release' }}"
          APK_PATH=$(find supermobile-app/android/app/build/outputs/apk/${BUILD_TYPE} -name "*.apk" | head -1)
          echo "APK found at: $APK_PATH"
          cp "$APK_PATH" superbrain.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: superbrain-${{ github.event.inputs.build_type || 'release' }}-${{ github.run_number }}
          path: superbrain.apk
          retention-days: 30
